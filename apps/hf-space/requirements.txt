# GS Mapper zero-install demo — Hugging Face Space dependencies.
# gsplat ships prebuilt CUDA wheels per torch/CUDA combo; keep the index in
# sync with the pinned torch below. Available combos are listed at
# https://docs.gsplat.studio/whl/<ptXXcuYYY>/gsplat/ (pt24cu121 and pt24cu124
# as of 2026-06).
--extra-index-url https://docs.gsplat.studio/whl/pt24cu121
torch==2.4.1
torchvision==0.19.1
gsplat==1.5.3+pt24cu121

gradio>=5.0
spaces

# gs-mapper itself (pulls numpy/opencv/Pillow/tqdm/pyyaml/...)
gs-mapper @ git+https://github.com/rsasaki0109/3dgs-robotics.git@main

# DUSt3R runtime deps (the clone happens at app startup)
numpy==1.26.4
opencv-python-headless
roma
einops
scipy
trimesh
safetensors
huggingface-hub

# allow gsplat to JIT-compile if no prebuilt wheel matches the runtime
ninja
